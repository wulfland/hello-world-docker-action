name: 'Wulfland Action'
description: 'Get started with Container actions'
author: '@wulfland'
branding:
  icon: 'award'  
  color: 'green'
inputs: 
  myInput:
    description: 'Input to use'
    default: 'world'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.myInput }}
