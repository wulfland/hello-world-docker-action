name: 'Wulfland Action'
description: 'Get started with Container actions'
author: '@wulfland'
inputs: 
  myInput:
    description: 'Input to use'
    default: 'world'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.myInput }}
